import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(initialRoute: 'register', routes: {
    'register': (context) {
      return Container();
    },
    'home': (context) {
      return Container();
    },
    'order': (context) {
      return Container();
    },
  }));
}
